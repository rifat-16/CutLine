Place the Firebase config for the staging Android app here.

Expected file:

`android/app/src/staging/google-services.json`

This config must belong to the Firebase Android app registered for the
`com.cutline.staging` package name in the `cutline-526aa` Firebase project.
