Place the Firebase config for the production Android app here.

Expected file:

`android/app/src/prod/google-services.json`

This config must belong to the Firebase Android app registered for the
`com.cutline` package name in the `cutline-prod-a55b9` Firebase project.
