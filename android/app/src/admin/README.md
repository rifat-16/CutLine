Place the Firebase config for the admin Android app here.

Expected file:

`android/app/src/admin/google-services.json`

This config must belong to the Firebase Android app registered for the
`com.cutline.admin` package name in the production Firebase project.
