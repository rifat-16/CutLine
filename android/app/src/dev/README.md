Place the Firebase config for the dev Android app here.

Expected file:

`android/app/src/dev/google-services.json`

This config must belong to the Firebase Android app registered for the
`com.cutline.dev` package name in the `cutline-dev` Firebase project.
