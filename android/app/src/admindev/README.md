Place the Firebase config for the dev admin Android app here.

Expected file:

`android/app/src/admindev/google-services.json`

This config must belong to the Firebase Android app registered for the
`com.cutline.admin_dev` package name in the `cutline-dev` Firebase project.
