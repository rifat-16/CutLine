rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthenticated() && request.auth.token.superadmin == true;
    }

    function isSalonVerifiedByData(data) {
      // Backwards-compat: older salon docs might not have this field yet.
      return !('verificationStatus' in data) || data.verificationStatus == 'verified';
    }

    function salonAdminManagedFields() {
      return [
        'verificationStatus',
        'submittedAt',
        'reviewedAt',
        'reviewedBy',
        'reviewNote',
        'isRestricted',
        'restrictionReason',
        'restrictedAt',
        'restrictedBy',
        'unrestrictedAt',
        'unrestrictedBy'
      ];
    }

    function isSalonRestrictedByData(data) {
      return ('isRestricted' in data) && data.isRestricted == true;
    }

    function isSalonOperationalByData(data) {
      return isSalonVerifiedByData(data) && !isSalonRestrictedByData(data);
    }

    function isSalonVerified(salonId) {
      return exists(/databases/$(database)/documents/salons/$(salonId))
        && isSalonVerifiedByData(get(/databases/$(database)/documents/salons/$(salonId)).data);
    }

    function isSalonOperational(salonId) {
      return exists(/databases/$(database)/documents/salons/$(salonId))
        && isSalonOperationalByData(get(/databases/$(database)/documents/salons/$(salonId)).data);
    }

    function isSalonOwnerId(salonId) {
      return isAuthenticated() && request.auth.uid == salonId;
    }

    function userRole(uid) {
      return exists(/databases/$(database)/documents/users/$(uid))
        ? get(/databases/$(database)/documents/users/$(uid)).data.role
        : null;
    }

    function isOwner(uid) {
      return userRole(uid) == 'owner';
    }

    function isBarber(uid) {
      return userRole(uid) == 'barber';
    }

    function barberOwnerId(barberUid) {
      return exists(/databases/$(database)/documents/users/$(barberUid))
        ? get(/databases/$(database)/documents/users/$(barberUid)).data.ownerId
        : null;
    }

    function isOwnerOrBarberOfSalon(salonId) {
      return isSalonOwnerId(salonId)
        || (isBarber(request.auth.uid) && barberOwnerId(request.auth.uid) == salonId);
    }

    function salonOwner(salonId) {
      return exists(/databases/$(database)/documents/salons/$(salonId)) 
        ? get(/databases/$(database)/documents/salons/$(salonId)).data.ownerId 
        : null;
    }

    function isValidStatus(status) {
      return status in ['pending', 'called', 'served', 'cancelled'];
    }

    function userActiveBookingIds(userId) {
      return exists(/databases/$(database)/documents/users/$(userId))
        && ('activeBookingIds' in get(/databases/$(database)/documents/users/$(userId)).data)
          ? get(/databases/$(database)/documents/users/$(userId)).data.activeBookingIds
          : [];
    }

    function userHasNoActiveBookings(userId) {
      return userActiveBookingIds(userId).size() == 0;
    }

    function isSelfCustomerBookingCreate() {
      return request.resource.data.userId == request.auth.uid
        || request.resource.data.customerUid == request.auth.uid;
    }

    function onlyChanges(allowedKeys) {
      return request.resource.data.diff(resource.data).changedKeys().hasOnly(allowedKeys);
    }

    function isCustomerNextFreeQueueCreate(salonId, queueId) {
      return isAuthenticated()
        && request.auth.uid == request.resource.data.customerUid
        && request.resource.data.entrySource == 'app'
        && request.resource.data.bookingMode == 'next_free'
        && request.resource.data.createdByUid == request.auth.uid
        && request.resource.data.createdByRole == 'customer'
        && request.resource.data.status == 'waiting'
        && request.resource.data.salonId == salonId
        && existsAfter(/databases/$(database)/documents/salons/$(salonId)/bookings/$(queueId));
    }

    match /users/{userId} {
      // Users can read their own document
      // Owners can read customer user documents to display avatars
      // Barbers can read their own document
      // Customers can read barber user documents to display barber profiles/avatars
      allow read: if isAuthenticated() && (
        isAdmin() ||
        request.auth.uid == userId ||
        isOwner(request.auth.uid) ||
        isBarber(request.auth.uid) ||
        true  // Allow all authenticated users to read user documents for displaying profiles/avatars
      );
      allow create, update: if isAuthenticated() && request.auth.uid == userId;
      allow delete: if false;
      
      // Nested favorites collection
      match /favorites/{salonId} {
        // Users can read/write their own favorites
        allow read, write: if isAuthenticated() && request.auth.uid == userId;
      }

      // Nested bookings mirror for user history
      match /bookings/{bookingId} {
        allow read, write: if isAuthenticated() && request.auth.uid == userId;
      }
    }

    match /salons/{salonId} {
      allow read: if isAuthenticated()
        && (isAdmin() || isOwnerOrBarberOfSalon(salonId) || isSalonVerifiedByData(resource.data));

      allow create: if isAuthenticated()
        && isOwner(request.auth.uid)
        && salonId == request.auth.uid
        && request.resource.data.ownerId == request.auth.uid
        && request.resource.data.verificationStatus == 'pending'
        && request.resource.data.isOpen == false
        && (!('isRestricted' in request.resource.data) || request.resource.data.isRestricted == false)
        && !request.resource.data.keys().hasAny([
          'reviewedAt',
          'reviewedBy',
          'reviewNote',
          'restrictionReason',
          'restrictedAt',
          'restrictedBy',
          'unrestrictedAt',
          'unrestrictedBy'
        ]);

      function salonVerificationFields() {
        return salonAdminManagedFields();
      }

      function salonUnverifiedOwnerAllowedUpdateKeys() {
        return [
          'name',
          'address',
          'mapAddress',
          'contact',
          'email',
          'ownerName',
          'description',
          'photoUrl',
          'coverImageUrl',
          'coverPhoto',
          'coverPhotoUrl',
          'galleryPhotos',
          'gallery',
          'photos',
          'galleryImages',
          'barbers',
          'updatedAt'
        ];
      }

      function ownerUpdateAllowed() {
        let changes = request.resource.data.diff(resource.data).changedKeys();
        let touchesVerification = changes.hasAny(salonVerificationFields());
        let verified = isSalonVerifiedByData(resource.data);
        return isSalonOwnerId(salonId)
          && !touchesVerification
          // Prevent going "open" before verification.
          && (verified || (!('isOpen' in request.resource.data) || request.resource.data.isOpen == false))
          // While not verified, only allow basic profile edits.
          && (verified || changes.hasOnly(salonUnverifiedOwnerAllowedUpdateKeys()));
      }

      function bookingBarberId(data) {
        return ('barberId' in data && data.barberId is string && data.barberId != '')
          ? data.barberId
          : (('barberUid' in data && data.barberUid is string) ? data.barberUid : '');
      }

      function bookingBarberName(data) {
        return ('barberName' in data && data.barberName is string && data.barberName != '')
          ? data.barberName
          : (('barber' in data && data.barber is string) ? data.barber : '');
      }

      function barberDisplayName(uid) {
        return exists(/databases/$(database)/documents/users/$(uid))
          ? (get(/databases/$(database)/documents/users/$(uid)).data.name is string
              ? get(/databases/$(database)/documents/users/$(uid)).data.name
              : '')
          : '';
      }

      function isAssignedBarberForBookingData(data, uid) {
        let assignedId = bookingBarberId(data);
        let assignedName = bookingBarberName(data);
        let actorName = barberDisplayName(uid);
        return (assignedId != '' && assignedId == uid)
          || (assignedName != '' && actorName != '' && assignedName == actorName);
      }

      allow update: if isAuthenticated() && ownerUpdateAllowed();
      allow delete: if isAuthenticated() && salonId == request.auth.uid;
      
      // Nested queue collection
      match /queue/{queueId} {
        // Owner can read/write their salon's queue
        // Barber can read their owner's salon queue
        // Customers can read queue to see wait times and live queue status
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId) || isSalonOperational(salonId));

        allow create: if isAuthenticated()
          && isSalonOperational(salonId)
          && (
            isOwnerOrBarberOfSalon(salonId) ||
            isCustomerNextFreeQueueCreate(salonId, queueId)
          );

        allow update, delete: if isAuthenticated()
          && isSalonOperational(salonId)
          && isOwnerOrBarberOfSalon(salonId);
      }
      
      // Nested bookings collection
      match /bookings/{bookingId} {
        // Keep current behavior (readable for live queue), but only after verification.
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId) || isSalonOperational(salonId));

        allow create: if isAuthenticated()
          && isSalonOperational(salonId)
          && (!isSelfCustomerBookingCreate() || userHasNoActiveBookings(request.auth.uid))
          && (
            salonId == request.auth.uid ||
            request.resource.data.userId == request.auth.uid ||
            request.resource.data.customerUid == request.auth.uid ||
            (isBarber(request.auth.uid) && barberOwnerId(request.auth.uid) == salonId)
          );

        allow update: if isAuthenticated()
          && isSalonOperational(salonId)
          && (
            salonId == request.auth.uid ||
            resource.data.userId == request.auth.uid ||
            resource.data.customerUid == request.auth.uid ||
            (
              isBarber(request.auth.uid)
              && barberOwnerId(request.auth.uid) == salonId
              && (
                isAssignedBarberForBookingData(resource.data, request.auth.uid) ||
                isAssignedBarberForBookingData(request.resource.data, request.auth.uid)
              )
            )
          );

        allow delete: if isAuthenticated()
          && isSalonOperational(salonId)
          && (
            salonId == request.auth.uid ||
            (
              isBarber(request.auth.uid)
              && barberOwnerId(request.auth.uid) == salonId
              && isAssignedBarberForBookingData(resource.data, request.auth.uid)
            )
          );
      }
      
      // Nested barbers collection (for barber availability)
      match /barbers/{barberId} {
        // Anyone authenticated can read barbers (customers need to see barbers for booking)
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId) || isSalonVerified(salonId));

        allow create, update, delete: if isAuthenticated()
          && (
            salonId == request.auth.uid ||
            (
              isSalonOperational(salonId)
              && barberId == request.auth.uid
            )
          );
      }

      // Owner-only / assigned-barber-only credential state.
      match /barber_credentials/{barberId} {
        allow read, create, update: if isAuthenticated()
          && (
            isAdmin()
            || salonId == request.auth.uid
            || (
              barberId == request.auth.uid
              && barberOwnerId(request.auth.uid) == salonId
            )
          );

        allow delete: if isAuthenticated()
          && (isAdmin() || salonId == request.auth.uid);
      }

      // Nested photos collection (salon gallery)
      match /photos/{photoId} {
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId) || isSalonVerified(salonId));
        allow create, update, delete: if isAuthenticated()
          && salonId == request.auth.uid
          && isOwner(request.auth.uid);
      }

      // Nested services collection (salon services)
      match /all_services/{serviceId} {
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId) || isSalonVerified(salonId));
        allow create, update, delete: if isAuthenticated()
          && salonId == request.auth.uid
          && isOwner(request.auth.uid);
      }

      // Nested stats collection (owner dashboard aggregates)
      match /stats/{dateKey} {
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId));
        allow create, update, delete: if isAuthenticated()
          && salonId == request.auth.uid;
      }

      // Nested per-barber daily serial counters for manual queue entries
      match /serial_counters/{counterId} {
        allow read: if isAuthenticated()
          && (isAdmin() || isOwnerOrBarberOfSalon(salonId));
        allow create, update, delete: if isAuthenticated()
          && isSalonOperational(salonId)
          && isOwnerOrBarberOfSalon(salonId);
      }
    }

    match /salons_summary/{salonId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated()
        && isOwner(request.auth.uid)
        && request.auth.uid == salonId
        && (!('verificationStatus' in request.resource.data) || request.resource.data.verificationStatus == 'pending')
        && (!('isRestricted' in request.resource.data) || request.resource.data.isRestricted == false)
        && !request.resource.data.keys().hasAny([
          'reviewedAt',
          'reviewedBy',
          'reviewNote',
          'restrictionReason',
          'restrictedAt',
          'restrictedBy',
          'unrestrictedAt',
          'unrestrictedBy'
        ]);
      allow update: if isAuthenticated()
        && isOwner(request.auth.uid)
        && request.auth.uid == salonId
        && !request.resource.data.diff(resource.data).changedKeys().hasAny(salonAdminManagedFields());
      allow delete: if false;
    }

    match /supportRequests/{requestId} {
      allow create: if isAuthenticated()
        && isOwner(request.auth.uid)
        && request.resource.data.ownerId == request.auth.uid
        && request.resource.data.status == 'open';
      allow read: if isAuthenticated() && (
        isAdmin() ||
        resource.data.ownerId == request.auth.uid
      );
      allow update: if isAuthenticated() && (
        isAdmin() ||
        (
          resource.data.ownerId == request.auth.uid &&
          request.resource.data.ownerId == resource.data.ownerId &&
          request.resource.data.status == resource.data.status &&
          request.resource.data.adminNote == resource.data.adminNote &&
          request.resource.data.assignedAdminUid == resource.data.assignedAdminUid &&
          request.resource.data.resolvedBy == resource.data.resolvedBy &&
          request.resource.data.resolvedAt == resource.data.resolvedAt
        )
      );
      allow delete: if false;
    }

    match /platform_fee/{docId} {
      allow read: if isAuthenticated();
      allow write: if false;
    }

    match /bookings/{bookingId} {
      allow read: if isAuthenticated() && (
        isAdmin() ||
        resource.data.userId == request.auth.uid ||
        salonOwner(resource.data.salonId) == request.auth.uid
      );

      allow create: if isAuthenticated()
        && isSalonOperational(request.resource.data.salonId)
        && request.resource.data.userId == request.auth.uid
        && userHasNoActiveBookings(request.auth.uid)
        && isValidStatus(request.resource.data.status);

      allow update: if isAuthenticated()
        && (
          isAdmin() ||
          resource.data.userId == request.auth.uid ||
          salonOwner(resource.data.salonId) == request.auth.uid
        )
        && isSalonOperational(resource.data.salonId)
        && isValidStatus(request.resource.data.status);

      allow delete: if false;
    }

    match /notifications/{notificationId} {
      allow read: if isAuthenticated() && (
        isAdmin() ||
        resource.data.userId == request.auth.uid
      );
      allow create: if false; // Only Cloud Functions can create
      allow update: if isAuthenticated() && resource.data.userId == request.auth.uid;
      allow delete: if isAuthenticated() && resource.data.userId == request.auth.uid;
    }

    match /barber_tip_ledger/{entryId} {
      allow read: if isAuthenticated() && (
        isAdmin() ||
        (isOwner(request.auth.uid) && request.auth.uid == resource.data.salonId) ||
        (isBarber(request.auth.uid) && request.auth.uid == resource.data.barberId)
      );
      allow create: if isAuthenticated() && (
        (isOwner(request.auth.uid) && request.resource.data.salonId == request.auth.uid) ||
        (isBarber(request.auth.uid) && barberOwnerId(request.auth.uid) == request.resource.data.salonId)
      );
      allow update: if isAuthenticated() && (
        (isOwner(request.auth.uid) && request.auth.uid == resource.data.salonId) ||
        (
          isBarber(request.auth.uid)
          && request.auth.uid == resource.data.barberId
          && request.resource.data.barberId == resource.data.barberId
          && request.resource.data.salonId == resource.data.salonId
          && onlyChanges(['status', 'confirmedAt'])
        )
      );
      allow delete: if false;
    }

    match /platform_fee_ledger/{entryId} {
      allow read: if isAuthenticated() && (
        isAdmin() ||
        (isOwner(request.auth.uid) && request.auth.uid == resource.data.salonId) ||
        (isBarber(request.auth.uid) && barberOwnerId(request.auth.uid) == resource.data.salonId)
      );
      allow create: if isAuthenticated() && (
        (isOwner(request.auth.uid) && request.resource.data.salonId == request.auth.uid) ||
        (isBarber(request.auth.uid) && barberOwnerId(request.auth.uid) == request.resource.data.salonId)
      );
      allow update: if isAuthenticated()
        && isOwner(request.auth.uid)
        && request.auth.uid == resource.data.salonId;
      allow delete: if false;
    }

    match /barber_payouts/{payoutId} {
      allow read: if isAuthenticated() && (
        isAdmin() ||
        (isOwner(request.auth.uid) && request.auth.uid == resource.data.salonId) ||
        (isBarber(request.auth.uid) && request.auth.uid == resource.data.barberId)
      );
      allow create: if isAuthenticated()
        && isOwner(request.auth.uid)
        && request.resource.data.salonId == request.auth.uid;
      allow update: if isAuthenticated()
        && isBarber(request.auth.uid)
        && request.auth.uid == resource.data.barberId
        && request.resource.data.barberId == resource.data.barberId
        && request.resource.data.salonId == resource.data.salonId
        && onlyChanges(['status', 'confirmedAt']);
      allow delete: if false;
    }

    match /platform_fee_payments/{paymentId} {
      allow read: if isAuthenticated()
        && (
          isAdmin() ||
          (
            isOwner(request.auth.uid)
            && request.auth.uid == resource.data.salonId
          )
        );
      allow create: if isAuthenticated()
        && isOwner(request.auth.uid)
        && request.resource.data.salonId == request.auth.uid;
      allow update, delete: if false;
    }

    match /admin_audit_logs/{logId} {
      allow read: if isAdmin();
      allow create, update, delete: if false;
    }
    
    // Top-level queue collection (fallback)
    match /queue/{queueId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated() && isOwner(request.auth.uid);
    }
  }
}
